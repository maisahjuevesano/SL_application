import { ImageContainer, NotFoundContainer } from "../styled/styledNotFound";

export const NotFound = () => {
  return (
    <NotFoundContainer>
      <ImageContainer />
    </NotFoundContainer>
  );
};
